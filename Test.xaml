<Window xmlns:ig="http://schemas.infragistics.com/xaml"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:UControl="clr-namespace:BoardDesigner.UControl"  x:Class="BoardDesigner.Test"
        Title="Test" Height="1080" Width="1920">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="500"/>
        </Grid.ColumnDefinitions>
        <Canvas>       
          <UControl:BoardImage x:Name="Img" />


        </Canvas>
        <DockPanel Grid.Column="1" >
            <ig:XamPropertyGrid HorizontalAlignment="Left"  VerticalAlignment="Top" ScrollBarVisibility="Auto" x:Name="xamPropertyGrid" 
                              
                                />
        </DockPanel>




    </Grid>
</Window>
